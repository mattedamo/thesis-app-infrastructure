apiVersion: v1
kind: Service
metadata: 
  name: backend-service
  labels:
    app: thesis-app
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: thesis-app
    tier: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
